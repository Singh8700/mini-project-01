<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>post edit</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/flowbite@2.5.2/dist/flowbite.min.css" rel="stylesheet" />
</head>
<body>
    <!-- edit form -->
    <div class="container m-auto flex justify-center items-center w-full px-8 flex-col w-1/4" id="add">
        
        <!-- new post form -->
       <div class="form w-full absolute top-0  h-full flex flex-col justify-center items-center backdrop-blur flex" id="close">
        <div class="icon absolute top-5 right-0 pr-5">
            <a href="/user/post">
            <span class="cursor-pointer text-zinc-400">Close</span>
        </a>
        </div>
        <form class=" flex flex-col justify-center items-center  " action="/user/editPost/<%= data._id %>" method="post">
            <input 
            type="text"
            class="mb-4 bg-transparent text-zinc-100 rounded-md  sm:w-[600px] md:w-[780px] lg:w-[1080px] 2xl:w-[1280px] p-3"
            placeholder="Title"
            name="title"
            id="title"
            required
            value="<%= data.title %>"
            >
            <textarea 
            placeholder="What's on your mind ?"
            class="resize-none sm:w-[600px] md:w-[780px] lg:w-[1080px] 2xl:w-[1280px]
            h-[400px] p-3 bg-transparent border-2 border-zinc-700 rounded-md mb-10 text-zinc-200" 
            name="post" 
            id="post"
            required
            ><%= data.post %></textarea>
            <button 
            class="text-yellow-300 bg-zinc-900 w-1/4 py-3 px-2 rounded-xl border"
            type="submit" 
            value="Create a Post"
            onclick="check()">Update Post</button>
        </form>
       </div>
    </div>
    <script src="/js/main.js"></script>
</body>
</html>